server:
    port: 8088
spring:
    application:
        name: SERVICE-CLIENT
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: pass123
        url: jdbc:mysql://localhost:3306/eureka?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
        username: root1
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true
